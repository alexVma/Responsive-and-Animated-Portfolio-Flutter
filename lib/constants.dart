import 'package:flutter/material.dart';

const primaryColor = Color(0xFFffc71f);
const secondaryColor = Color(0xFF3d4b59);
const darkColor = Color(0xFF191923);
const bodyTextColor = Color(0xFF8B8B8D);
const bgColor = Color(0xFF283747);

const defaultPadding = 20.0;
const defaultDuration = Duration(seconds: 2); // we use it on our animation
const maxWidth = 1440.0; // max width of our web
